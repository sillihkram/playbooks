---
- name: Clone VMs on standalone ESXi (AAP safe)
  hosts: localhost
  connection: local
  gather_facts: no

  vars:
    vmware_hostname: 192.168.1.20
    vmware_username: root
    vmware_password: "{{ vmware_password }}"

    datacenter: ha-datacenter
    folder: "/ha-datacenter/vm"

    source_vm: "RHEL9-CIS_L1"

    vms:
      - graffias.openincite.net
      - acrab.openincite.net
      - jabbah.openincite.net
      - alniyat.openincite.net
      - apollyon.openincite.net
      - shaula.openincite.net
      - lesath.openincite.net

  tasks:

    - name: Clone VM from existing golden VM (correct ESXi method)
      community.vmware.vmware_guest:
        hostname: "{{ vmware_hostname }}"
        username: "{{ vmware_username }}"
        password: "{{ vmware_password }}"
        validate_certs: false
        port: 443

        datacenter: "{{ datacenter }}"
        folder: "{{ folder }}"

        name: "{{ item }}"
        state: poweredoff

        is_template: false
        resource_pool: Resources

        clone_from_vm: "{{ source_vm }}"

        hardware:
          memory_mb: 8192
          num_cpus: 4

      loop: "{{ vms }}"
      delegate_to: localhost

    - name: Power on VMs
      community.vmware.vmware_guest_powerstate:
        hostname: "{{ vmware_hostname }}"
        username: "{{ vmware_username }}"
        password: "{{ vmware_password }}"
        validate_certs: false
        port: 443

        name: "{{ item }}"
        state: powered-on

      loop: "{{ vms }}"
      delegate_to: localhost
